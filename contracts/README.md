Use the following commands to deploy on Mantle:

```bash
npx hardhat compile
```
```bash
npx hardhat ignition deploy ./ignition/modules/<Your-Contract>.ts --network mantleSepolia
```

Then you will get a contract address and other details, which you can verify at: [Mantle Explorer](https://mantlescan.xyz/)